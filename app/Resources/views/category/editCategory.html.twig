{% for category in categories %}
    <span>{{ category.category }} </span>
    <a href="{{ path('deleteCategory', {'name': category.category}) }}">
        delete
    </a></br>
{% endfor %}
{{ form_start(form) }}
{{ form_widget(form) }}
{{ form_end(form) }}
<input type="submit"/>